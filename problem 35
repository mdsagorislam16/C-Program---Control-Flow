#include <stdio.h>

int main() {
    int n, sq, sum = 0;
    scanf("%d", &n);

    sq = n * n;

    for (; sq > 0; sq /= 10) {
        sum = sum + (sq % 10);
    }

    if (sum == n) {
        printf("Neon Number\n");
    }
    else {
        printf("Not Neon\n");
    }

    return 0;
}
