#include <stdio.h>

int main() {
    int l, r;
    scanf("%d %d", &l, &r);

    for (int n = l; n <= r; n++) {
        int prime = 1;

        if (n < 2) {
            prime = 0;
        }

        for (int i = 2; i * i <= n; i++) {
            if (n % i == 0) {
                prime = 0;
                break;
            }
        }

        if (prime == 1) {
            printf("%d ", n);
        }
    }

    return 0;
}
